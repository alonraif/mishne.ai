# samples

Drop real source material here — AAFs, video, audio.

**Nothing in this folder is committed** (see .gitignore) and nothing is uploaded
anywhere. The pipeline runs on this machine, against these files, in place.

```bash
cd apps/api
.venv/bin/python run.py ../../samples/YOUR_FILE.aaf --notes "..." --target 6m
```
